apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: models-storage
  namespace: NAMESPACE_PLACEHOLDER
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 10Gi