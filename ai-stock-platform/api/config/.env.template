# Database Configuration for RDS
POSTGRES_SERVER=your-rds-instance.region.rds.amazonaws.com
POSTGRES_USER=quantumvest
POSTGRES_PASSWORD=your-strong-password-here
POSTGRES_DB=quantumvestai
POSTGRES_PORT=5432

# AWS Configuration
AWS_REGION=us-east-1
USE_IAM_AUTH=false

# Set to true if using IAM authentication instead of password
# Requires proper IAM roles and policy setup
# USE_IAM_AUTH=true