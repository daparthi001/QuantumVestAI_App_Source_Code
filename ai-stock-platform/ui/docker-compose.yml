version: '3.8'

services:
  ui:
    build:
      context: .
      dockerfile: Dockerfile
    image: quantumvestai-ui:latest
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DEBUG=true
      - API_BASE_URL=http://quantumvestai-dev-api:8000
      - ENVIRONMENT=development
    volumes:
      - ./ui:/app/ui
      - ./templates:/app/templates
      - ./static:/app/static
      - ./scripts:/app/scripts
    depends_on:
      - api
      
  api:
    image: quantumvestai-api:latest  # You'll need to build this separately
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - DEBUG=true
      - ENVIRONMENT=development
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=quantumvestai
      - DB_USER=postgres
      - DB_PASSWORD=postgres
    depends_on:
      - db
      
  db:
    image: postgres:13
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=quantumvestai
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: